name: whatsapp
description: A top package for whatsapp business api send messages, product, media, location through flutter app.
version: 2.0.1
homepage: https://rohitchouhan.com
repository: https://github.com/rohit-chouhan/whatsapp

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter: